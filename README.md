**sigfox-aws-data** is a [`sigfox-aws`](https://www.npmjs.com/package/sigfox-aws) adapter for recording Sigfox messages into SQL databases

Moved to `sigfox-iot-data`:

https://www.npmjs.com/package/sigfox-iot-data
